import { Dimensions, StatusBar } from "react-native";

export const width = Dimensions.get("screen").width;
export const hright = Dimensions.get("screen").height;
export const statusBarHeight = StatusBar.currentHeight;